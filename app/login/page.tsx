import Link from "next/link";
import { Fish, LogIn } from "@/components/icons";
import { isSupabaseConfigured } from "@/lib/supabase/server";
export default function LoginPage() { const configured = isSupabaseConfigured(); return <main className="login-page"><div className="login-panel"><Link className="brand" href="/"><span className="brand-mark"><Fish /></span><span>Eliason Family Fish <em>Records</em></span></Link><div><span className="kicker">Family access</span><h1>Sign in to share a catch.</h1><p>Family members use an invited email address. We’ll send a secure, password-free sign-in link.</p></div>{!configured && <div className="setup-notice"><strong>Preview mode:</strong> authentication becomes available when Supabase is connected.</div>}<form action="/api/auth/magic-link" method="post"><label>Email address<input type="email" name="email" required /></label><button className="primary-button" disabled={!configured}><LogIn size={17} /> Email me a sign-in link</button></form><Link className="back-link" href="/">← Return to the archive</Link></div><div className="login-photo" /></main>; }
