import { describe, expect, it } from "vitest";
import { records } from "./data";
describe("legacy migration", () => { it("contains all standard and micro catches", () => expect(records).toHaveLength(135)); it("preserves expected archive dimensions", () => { expect(new Set(records.filter(r => r.status !== "micro").map(r => r.species))).toHaveLength(78); expect(new Set(records.map(r => r.species))).toHaveLength(81); expect(new Set(records.map(r => r.angler))).toHaveLength(13); expect(records.filter(r => r.status === "current")).toHaveLength(77); expect(records.filter(r => r.status === "micro")).toHaveLength(3); }); it("recovers verified photos from the supplied archive", () => expect(records.filter(r => r.photo)).toHaveLength(128)); it("labels estimated locations without overwriting exact coordinates", () => { expect(records.filter(r => r.coordinateAccuracy === "estimated")).toHaveLength(18); expect(records.filter(r => r.coordinateAccuracy === "unknown")).toHaveLength(2); expect(records.every(r => (r.lat === null) === (r.lng === null))).toBe(true); }); });
